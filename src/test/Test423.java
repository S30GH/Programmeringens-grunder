/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package test;

import java.util.*;

/**
 *
 * @author svph1820
 */
public class Test423 {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        //double deg = 47;
        //double result = Math.toRadians(deg);
        //double random = Math.random()*100;
        //int randomInt = (int) random;
        /* Övning 4.2.5
        Scanner input = new Scanner(System.in);
        System.out.print("Sätt minsta värdet på intervallet som ska slumpmässigt pluppa ut ett värde: ");
        int min = input.nextInt();
        System.out.print("Sätt högsta värdet på intervallet som ska slumpmässigt pluppa ut ett värde: ");
        int max = input.nextInt();
        int range = max - min;
        int rand = (int) (Math.random() * range + min);
        System.out.println("\"Slumpmässigt\" värde mellan " + min + " och " + max + " blev " + rand);
         */
        /* Övning 4.3.6 
        char x = 'a';
        char y = 'c';
        System.out.println(++x);
        System.out.println(y++);
        System.out.println(y);
        System.out.println(x - y);
        */
    }

}
